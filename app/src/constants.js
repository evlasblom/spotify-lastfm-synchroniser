// Keys used for local storage
export const state_key = 'lastify.spotify_auth_state';
export const token_key = 'lastify.spotify_auth_token';
export const user_key = 'lastify.lastmf_username';

// Debug toggle
// It allows, for example, to click on each item in the lists to print its details to the console
export const DEBUG = false;